for copying the elements to an "array" to "HashSet", you can use this in-built method, Use the HashSet<T>.CopyTo method.
This method copies the items from the HashSet<T> to an array. ----->

"HashSet.CopyTo(Array);"
